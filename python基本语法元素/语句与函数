1、语句与函数
    赋值语句、分支语句、循环语句
    函数：<函数名>(<参数>)
2、输入和输出
    input()：从控制台获取用户输入的函数
        <变量> = input(<提示信息字符串>)
    print()：以字符形式向控制台输出结果的函数
        print(<拟输出字符串或字符串变量>)
        print()函数的格式化：
            print("转换后的温度是{.2f}C".format(C))
3、评估函数eval()
    去掉参数最外侧引号并执行余下语句的函数
        eval(<字符串或字符串变量>)
4、循环语句
    for <变量> in range(<次数>):
        <被循环执行的语句>
    <变量> 从0到<次数>-1
        for i in range(5):
            print("Hello:",i)
5、range()函数：产生序列
    range(N):从 0 到 N-1
    range(M,N):从 M 到 N-1